database {
  postgres {
    connectionPool = "HikariCP"
    dataSourceClass = "org.postgresql.ds.PGSimpleDataSource"
    numThreads = 10

    properties = {
      serverName = localhost
      portNumber = 5434
      databaseName = "fun-cart"
      user = "postgres"
      password = "1111"
    }
  }
}