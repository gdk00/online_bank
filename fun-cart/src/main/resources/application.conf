database {
  postgres {
    connectionPool = "HikariCP"
    dataSourceClass = "org.postgresql.ds.PGSimpleDataSource"
    numThreads = 10

    properties = {
      serverName = ${PGHOST}
      portNumber = ${PGPORT}
      databaseName = "fun-cart"
      user = "postgres"
      password = "1111"
    }
  }
}

port = 8080